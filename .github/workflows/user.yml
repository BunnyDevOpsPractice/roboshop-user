name: user
on:
  push: { branches: [main] }
  workflow_dispatch:
jobs:
  ci:
    uses: bunnyrdy/ArgoCD_Project/.github/workflows/ci.yml@main
    with:
      language: user == node? -> node
      component: user
      valuesPath: charts/user-deploy/helm/values.yaml
    secrets:
      GITOPS_TOKEN: ${{ secrets.GITOPS_TOKEN }}
